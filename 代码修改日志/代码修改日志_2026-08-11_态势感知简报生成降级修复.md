# 代码修改日志：态势感知简报生成降级修复（2026-08-11）

## 问题

事件孪生态势感知面板在外部 LLM 兼容接口返回 HTTP 400 时，仅显示“简报未生成”，影响值班人员查看事件概况、交通影响和处置进展。

## 修复内容

| 范围 | 修改内容 |
| --- | --- |
| `demo/src/services/llm.ts` | HTTP 400 时自动移除 `response_format` 后重试一次；失败信息会保留接口返回的具体原因。新增本地规则简报构建器。 |
| `demo/src/store.ts` | 缺少配置、调用失败或输出校验失败时，保存“本地规则引擎”简报并写入时间片历史，页面状态保持可用。 |
| `demo/src/services/llm.test.ts` | 新增本地规则简报结构校验用例；保留兼容端点重试用例。 |
| `demo/README.md` | 补充简报重试与本地降级机制说明。 |

## 降级口径

本地规则简报只使用当前模拟事件、交通流模型、门架通行能力、雷视融合演示目标和已确认处置措施。内容明确标记为模拟与模型结果，不替代现场实况或人工复核。

## 验证

```bash
cd demo
npm.cmd run check
```

验证结果：Oxlint 通过；Vitest 38 个测试文件、182 项用例通过；TypeScript 类型检查与 Vite 生产构建通过。
