# 代码修改日志：测试验收与高德重试修复（2026-08-05）

## 变更摘要

完成本地演示工作台的代码、构建、依赖与浏览器端功能验收；修复高德 JS API 在首次网络加载失败后不能再次尝试加载的问题。

## 代码变更

| 文件 | 变更 | 原因 |
|---|---|---|
| `demo/src/gis/amapLoader.ts` | 脚本 `onerror` 触发时将模块级 `loadingPromise` 置空。 | 避免 rejected Promise 长驻，支持网络恢复或组件重新挂载后的再次加载。 |
| `demo/src/gis/amapLoader.test.ts` | 新增加载失败后重试的回归用例。 | 防止失败缓存逻辑回归；测试同时注明浏览器脚本加载事件的异步时序。 |
| `demo/README.md` | 新增“最近一次测试验收（2026-08-05）”章节。 | 记录可复现命令、质量结果、浏览器冒烟范围和整改结论。 |

## 验证记录

| 项目 | 命令/方法 | 结果 |
|---|---|---|
| 高德重试专项回归 | `npm.cmd run test -- src/gis/amapLoader.test.ts` | 通过，1 项用例。 |
| 质量门禁 | `npm.cmd run check` | 通过：Oxlint 无告警；36 个测试文件、170 项用例通过；TypeScript 与 Vite 生产构建通过。 |
| 依赖审计 | `npm.cmd audit --offline --audit-level=moderate` | 通过，0 vulnerabilities。 |
| 浏览器冒烟 | 加载案例一并检查主工作台、GIS、交通能力和时序 | 通过，未发现项目控制台错误。 |

## 影响与回退

- 影响范围仅限高德 JS API 脚本加载失败后的重试状态，不改变已成功加载的地图实例、覆盖物、数据模型或业务推演逻辑。
- 如需回退，删除 `script.onerror` 中的 `loadingPromise = null` 即恢复原先的失败缓存行为；不建议回退该项可靠性修复。
