# 代码修改日志：Arco 设计规范视觉升级

日期：2026-07-26  
范围：`demo` 前端运行工作台

## 设计依据

- Figma 文件：Arco Design System Community Copy
- 参考节点：`115962:128330`（组件索引）、`120463:106317`（Card）
- 核心规范：白色卡面、8px 卡片圆角、1px 分隔线、#4080FF 主色、#1D2129 主文字、#E5E6E8 边框与 48px 标题栏。

## 本次修改

1. 更新全局设计令牌
   - 将主色切换为 Arco `#4080FF`，补充浅蓝层级与统一边框色。
   - 调整焦点态、按钮、输入框、卡片、标签和滚动条的视觉层级。
2. 统一工作台主框架
   - `PanelFrame` 统一为 8px 圆角、低层级阴影、白色标题栏、48px 标题区和 16px 水平内边距。
   - 四列工作台间距统一为 16px，并同步优化中等宽度屏幕下的列宽与间距。
3. 优化核心业务入口
   - 事件实时态势感知、上下游门架能力面板同步采用新卡片与标题区规范。
   - 事件列表选中态改为更清晰的浅蓝反馈面、主色描边与 8px 圆角。
   - 顶部时间轴高度调整为 64px，品牌标识改为 4px 圆角。

## 验证口径

- 执行 `npm.cmd run check`：Oxlint、Vitest、TypeScript 构建与 Vite 生产构建均应通过。
- 视觉改造仅调整样式、布局和组件外观，不改变事件、预案、仿真、LLM 或处置状态机业务逻辑。
