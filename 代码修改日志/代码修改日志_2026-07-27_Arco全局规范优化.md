# Arco 全局规范优化（2026-07-27）

## 调整范围

- 以 Figma `Arco Design System Community Copy` 的 Card 节点 `120463:106317` 为实现基线，统一全局设计令牌。
- 调整中文、英文和数值字体回退链；补齐中性文字色阶与图标对齐样式。
- 统一按钮、输入框、标签、提示、卡片、标题栏、焦点态和滚动条的视觉基线。
- 工作台四列间距统一为 `12px`，常规业务卡片去除装饰性阴影，保留边框和层级关系。
- 增加 `1181px–1366px` 紧凑桌面栅格，并为 GIS 标题提供紧凑文案，避免中等屏幕下标题换行。
- 顶部设置、数据集、审计及列表、图表、全屏操作采用统一的图标尺寸和 Arco 按钮规格。
- 修复态势简报低风险标签引用不存在颜色令牌的问题，改为成功语义色。

## 验证

执行 `npm.cmd run check`：Oxlint、Vitest、TypeScript 与 Vite 生产构建均应通过。
